version: '3.8'

services:
  telegram-bot:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./data:/var/www/html/data
      - ./cache:/var/www/html/cache
    restart: always
